language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-template-frontend' ]; then cd .. && eval "mv $currentfolder generator-template-frontend" && cd generator-template-frontend; fi

