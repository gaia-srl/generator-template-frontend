/* global describe, it, expect */

(function () {
    'use strict';

    describe('main.js test example', function () {
        it('should define an object called app', function () {
            expect(app).toBeDefined();
        });
        
    });
})();
